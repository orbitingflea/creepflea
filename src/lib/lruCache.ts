/**
 * 本文件实现了 Least Recently Used 缓存处理算法，并封装成 class LRUCache，供其他地方调用
 * 使用时可以比较容易地把 class instance 封装进 global 内，作为一个对象
 */

// import LinkedMap from 'goog:goog.structs.LinkedMap';



// export class LRUCache<Value> {
//   table: LinkedMap<string, Value>;
//   constructor(nMax: number) {
//     this.table = new LinkedMap(nMax, true);
//   }
//   get(key: string): Value | undefined {
//     return this.table.get(key);
//   }
//   set(key: string, value: Value): void {
//     this.table.set(key, value);
//   }
// };
